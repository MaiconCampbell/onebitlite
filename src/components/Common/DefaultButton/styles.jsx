import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  button: {
    borderWidth: 1,
    borderColor: '#FFF',
    borderRadius: 10,
    marginBottom: 20,
    padding: 8,
  },
  buttonText: {
    color: '#FFF',
    fontWeight: 'bold',
    fontSize: 20,
    textAlign: 'center'
  }
})

export default styles